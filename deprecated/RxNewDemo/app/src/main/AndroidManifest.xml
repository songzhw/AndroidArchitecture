<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.six.demo">

    <application android:icon="@drawable/ic_launcher" android:roundIcon="@drawable/ic_launcher_round"
        android:label="@string/app_name" android:theme="@style/AppTheme"
        android:name=".dagger.whole.core.WholeApp">

        <activity android:name=".dagger.whole.biz.list.PetListActivity" android:label="Home Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".dagger.whole.biz.detail.PetDetailActivity"/>

        <activity android:name=".dagger.scope.DemoB"/>
        <activity android:name=".dagger.scope.c.DemoC"/>


    </application>

</manifest>